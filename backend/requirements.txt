# ============================================================
# QuantEdge v6.0 — Python Requirements
# © 2024–2025 Dileep Kumar Reddy Kapu. All Rights Reserved.
#
# NOTE: Production Docker build uses explicit pip install commands
# in the Dockerfile (not pip install -r requirements.txt) for
# better layer caching. This file is for local dev and reference.
#
# Local dev: pip install -r requirements.txt
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0
requests==2.31.0
aiohttp==3.9.5
websockets==12.0

# ── Auth & AWS ───────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
boto3==1.34.69
cryptography==42.0.5
pyotp==2.9.0

# ── Database & Cache ─────────────────────────────────────────
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.29
alembic==1.13.1
redis==5.0.3
aioredis==2.0.1
psycopg2-binary==2.9.9

# ── Config & Utils ───────────────────────────────────────────
pydantic==2.7.0
pydantic-settings==2.2.1
loguru==0.7.2
tenacity==8.2.3
python-dotenv==1.0.1
celery[redis]==5.3.6
joblib==1.3.2

# ── Data & Market Feeds ──────────────────────────────────────
yfinance==0.2.38
pandas==2.2.1
numpy==1.26.4
scipy==1.13.0
fredapi==0.5.1
newsapi-python==0.2.7
praw==7.7.1

# ── ML Core ──────────────────────────────────────────────────
scikit-learn==1.4.2
xgboost==2.0.3
lightgbm==4.3.0
statsmodels==0.14.2
arch==6.3.0           # GJR-GARCH volatility
hmmlearn==0.3.2       # Hidden Markov Models (regime detection)
ta==0.11.0            # Technical analysis indicators
shap>=0.45.0          # Model explainability

# ── Optimization ─────────────────────────────────────────────
cvxpy==1.4.2          # Convex optimization for portfolio construction

# ── NLP / Sentiment ──────────────────────────────────────────
transformers==4.40.1  # FinBERT
tokenizers==0.19.1
vaderSentiment==3.3.2
textblob==0.18.0
nltk==3.8.1

# ── AI API ───────────────────────────────────────────────────
anthropic>=0.30.0     # Claude API

# ── Deep Learning ────────────────────────────────────────────
# Install torch separately with CPU-only wheel for Docker:
# pip install torch==2.2.2+cpu --index-url https://download.pytorch.org/whl/cpu
torch==2.2.2

# ── Testing ──────────────────────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.6
