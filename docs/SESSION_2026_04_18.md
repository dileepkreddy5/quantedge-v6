# QuantEdge V6 — Session Notes
**Date:** April 18, 2026
**Duration:** ~9 hours
**Ending state:** Production live, Overview V2 deployed, AAPL bug fixed

---

## What Got Built Tonight (13 commits)

### Backend endpoints shipped to production
1. `/api/v6/quality/{ticker}` — fundamentals quality engine
2. `/api/v6/screener/regime` — market regime classifier
3. `/api/v6/screener/all` — ranked universe across 3 horizons
4. `/api/v6/screener/{horizon}` — per-horizon rankings
5. `/api/v6/screener/ticker/{ticker}` — single ticker lookup
6. `/api/v6/screener/rescan` (POST) — force refresh
7. `/api/v6/portfolio/size` (POST) — risk-parity sizer
8. `/api/v6/portfolio/size_from_screener/{horizon}` — auto-size from ranking
9. `/api/v6/news_momentum/{ticker}` — analyst revision proxy

### Frontend shipped
- `/screener` standalone route (ranked universe page)
- Screener integrated as Dashboard tab (after ML MODELS)
- Watchlist moved to end of tabs
- OverviewV2 — Bloomberg-style layered-insight redesign
- `frontend/src/pages/overview_v2/insights.ts` — interpretation layer (362 lines)
- `frontend/src/pages/overview_v2/OverviewV2.tsx` — UI component (415 lines)

### Backtest infrastructure (local only, committed to repo)
- `backend/research/historical_data.py` — 5yr OHLCV cache
- `backend/research/fundamentals_cache.py` — point-in-time financials
- `backend/research/backtest_engine.py` — walk-forward engine
- `backend/research/performance_metrics.py` — institutional metrics
- `backend/research/out_of_sample.py` — train/test split harness
- `backend/research/long_horizon_test.py` — annual rebalance variant

### Critical bug fixes
- ESLint `react-hooks/exhaustive-deps` rule removed (unblocked deploys)
- `_sanitize_json()` helper added to /analyze endpoint (fixes NaN/inf crash on AAPL)

---

## Honest Backtest Findings

Tested 5 strategy configurations on 2022-2026 data with OOS split (train 2022-2024, test 2024-2026):

| Strategy | Train Alpha | Test Alpha | Sharpe Retention | Verdict |
|----------|-------------|------------|------------------|---------|
| short_term (monthly momentum) | +21.24% | +7.88% | 55% | ✅ REAL EDGE |
| medium_term (balanced, monthly) | +15.81% | +0.22% | 31% | ⚠️ MARGINAL |
| long_term (quarterly quality) | +8.58% | -4.03% | 15% | ❌ FAILED |
| patient_quality (annual, 70% qual) | +6.00% | -12.52% | negative | ❌ REGIME FAIL |
| random-signal control | — | -3.31% | — | ✅ Backtest unbiased |

**Realistic forward expectations:**
- Short-term strategy: 3-5% annualized alpha, 30-35% max drawdown expected
- Other configurations: essentially 0 edge, do not deploy
- Transaction costs consume 2-3% per year on monthly rebalance

**Capital allocation recommendation:**
- 60-70% SPY (boring, correct)
- 20-30% short-term strategy (only after 3-6 months paper trading)
- 5-10% cash/hedge dry powder

---

## ML Refactor Roadmap (FOR NEXT SESSION — NOT STARTED TONIGHT)

Audit from session start identified significant technical debt. Here's the map:

### Orphaned code confirmed in `backend/`
- `backend/ml/v6/` — `bridge.py`, `institutional_alpha_engine.py`, `institutional_risk_engine.py` (not imported by `/api/v6/analyze`)
- `backend/engine/` — entire parallel hierarchy (alpha/capital/data/governance/portfolio/regime/risk) appears unused
- `backend/ml/alpha_engine.py` — standalone, not wired in

### Sophisticated components existing but not wired correctly
- `backend/ml/labeling/triple_barrier.py` — López de Prado triple-barrier labels. Imported by `analysis_v6.py` but XGBoost/LightGBM use naive fixed-horizon labels instead
- `backend/ml/reasoning/claude_reasoning.py` — 4-agent InvestmentCommittee (may be wired, unclear)
- `backend/ml/portfolio/portfolio_engine.py` — HRP sizing (partial wiring found earlier)

### Known issues in production pipeline
- 80/20 split instead of walk-forward CV in `analysis_v6.py` training
- ±30% return clip destroys signal in training data
- ML models retrained per request (slow, non-reproducible)
- Risk engine throws "zero-size array" warning on every ticker (caught as warning, but risk tab empty)

### Suggested session sequence (budget 4-5 hrs per session, fresh only)

**Session 1: Dead-code audit (4-5 hr)**
- Walk every ML file; build dependency graph
- Produce `ml/ARCHITECTURE.md` mapping: orphaned / partial / wired
- No code changes, just documentation

**Session 2: Triple-barrier wiring (5-6 hr)**
- Connect `LabelingPipeline` to `XGBoost/LightGBM` training
- Expected gain: biggest single ML improvement possible

**Session 3: Walk-forward CV + training pipeline (4-5 hr)**
- Replace 80/20 with walk-forward
- Remove ±30% return clip
- Pre-train once, cache, don't retrain per request

**Session 4: InvestmentCommittee wiring (3-4 hr)**
- Connect 4-agent system to analyze endpoint
- This is what makes the signal genuinely differentiated

**Session 5: Validation (2-3 hr)**
- Re-run backtest suite with new pipeline
- Compare OOS alpha: new vs old

**Total: 18-25 hours of focused work across ~2-3 weeks of evenings**

---

## Open Issues (Not Blocking)

1. **Risk engine "zero-size array" warning** on every ticker. Currently suppressed. Should be fixed but analysis completes without it.
2. **ML model "not converging" warnings** on some tickers (META seen in logs). Models still produce output but convergence is marginal.
3. **NaN/inf in analysis output** — sanitizer patches the symptom. Root cause is somewhere in feature engineering or ML prediction (unidentified).

---

## Site State (as of end of session)

- **Live URL:** https://quant.dileepkapu.com
- **Working tickers:** AAPL (fixed tonight), NVDA, MSFT, TSLA, SPY, QQQ, AMZN, META, GOOGL, BRK-B
- **Dashboard tabs:** OVERVIEW (V2) | ML MODELS | SCREENER | VOLATILITY | REGIME | SENTIMENT | MONTE CARLO | RISK | FUNDAMENTALS | WALL ST. | PORTFOLIO | PERFORMANCE | WATCHLIST
- **Standalone pages:** /screener (also embedded in Dashboard)

---

## What NOT to Do Next

- Don't deploy real capital without 3-6 months of paper trading
- Don't add more factor weights or strategies before fixing the ML pipeline
- Don't rewrite OverviewV2 — it's fine
- Don't add more UI features before proving backend generates real alpha

## What TO Do Next (in order of importance)

1. **Paper trade for 3-6 months** — open Alpaca paper account, run the short-term strategy monthly, log results
2. **ML refactor Session 1** — dead-code audit (see roadmap above)
3. **Consider QUAL ETF** as the "boring correct" allocation for 60-70% of capital
4. **Decide capital allocation posture** — full DIY vs passive-core + alpha-sleeve

---

*End of session notes. Generated 2026-04-18. Written by Claude, co-built with Dileep.*
